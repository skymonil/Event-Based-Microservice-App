{
  "name": "synthetic-tests",
  "version": "1.0.0",
  "description": "Synthetic Smoke & E2E Tests for Argo Analysis",
  "main": "index.js",
  "scripts": {
    "test": "jest --verbose",
    "test:user": "jest user-service --runInBand --detectOpenHandles --testTimeout=30000",
    "test:order": "jest order-service --detectOpenHandles --testTimeout=30000",
  "test:order:smoke": "jest order-service/order-service.smoke.test.js --detectOpenHandles",
  "test:order:e2e": "jest order-service/order-service.full.e2e.test.js --detectOpenHandles",
    "test:inventory": "jest inventory-service --detectOpenHandles"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "axios-retry": "^4.0.0",
    "dotenv": "^16.3.0",
    "jest": "^29.7.0",
    "kafkajs": "^2.2.4",
    "uuid": "^9.0.0"
  }
}