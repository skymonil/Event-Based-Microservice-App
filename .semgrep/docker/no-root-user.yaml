rules:
  - id: no-root-user
    message: Containers must not run as root.
    severity: ERROR
    languages:
      - generic
    paths:
      include:
        - "**/Dockerfile"
        - "**/*.Dockerfile"
        - "docker/**"
    pattern-regex: '(?m)^\s*USER\s+root\s*$'