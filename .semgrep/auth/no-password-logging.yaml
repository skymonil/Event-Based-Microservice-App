rules:
  - id: no-password-logging

    message: Never log passwords or JWT tokens.

    severity: ERROR

    languages:
      - javascript

    pattern-either:
      - pattern: logger.$METHOD(..., password, ...)
      - pattern: logger.$METHOD(..., token, ...)
      - pattern: logger.$METHOD(..., password_hash, ...)