rules:
  - id: no-hardcoded-jwt-secret

    message: JWT secrets must come from environment variables.

    severity: ERROR

    languages:
      - javascript

    pattern: |
      jwt.sign(..., "$SECRET", ...)